# Pi-Kinect Configuration
# Copy this file to .env and update the values

# Windows PC IP address for streaming (NOT the Pi IP)
# The Pi IP is 192.168.1.9, so use a different IP for your Windows PC
PC_IP=192.168.1.100

# Repository directory on Pi (username: ls)
REPO_DIR=/home/ls/pi-kinect

# Optional: Override default Python app
# PY_APP=/home/ls/pi-kinect/kinect_robust.py

# Optional: GStreamer settings
# GST_FPS=20
# GST_QUALITY=70
